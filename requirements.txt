# --- core (required) ---------------------------------------------------
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
scipy>=1.10
matplotlib>=3.7
seaborn>=0.13
xgboost>=2.0
lightgbm>=4.1
joblib>=1.3
tabulate>=0.9          # used by pandas .to_markdown() when writing REPORT.md

# --- explainability ----------------------------------------------------
shap>=0.45

# --- optional: hyperparameter tuning (`--tune`) -------------------------
# Falls back to sklearn RandomizedSearchCV if absent.
optuna>=3.5

# --- optional: neural-net baseline --------------------------------------
# The pipeline skips the MLP cleanly if this isn't installed.
# tensorflow>=2.15

# --- optional: model serving (src/serve.py) -----------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# --- optional: notebook + tests ------------------------------------------
jupyter
notebook
pytest>=8.0
